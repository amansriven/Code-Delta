services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: codedelta
      POSTGRES_PASSWORD: codedelta
      POSTGRES_DB: codedelta
    ports:
      - "5432:5432"
    volumes:
      - codedelta_pgdata:/var/lib/postgresql/data

volumes:
  codedelta_pgdata:
