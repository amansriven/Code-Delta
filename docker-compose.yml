services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: deltacode
      POSTGRES_PASSWORD: deltacode
      POSTGRES_DB: deltacode
    ports:
      - "5432:5432"
    volumes:
      - deltacode_pgdata:/var/lib/postgresql/data

volumes:
  deltacode_pgdata:
