FROM docker.io/cloudflare/sandbox:0.12.4-python

# The image intentionally adds no project dependencies. Repository-controlled
# package installation is a separate, policy-gated command inside the sandbox.
